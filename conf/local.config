/*
 * Execucao local (servidor com GPU, 32 cores)
 */
params {
    ntomp = 16   // 1 rank MPI + 16 threads OpenMP — ideal para GPU offload
}

process {
    executor = 'local'
}

executor {
    cpus   = 32
    memory = '64.GB'
}
